import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <h1>Ứng dụng React của tôi đã chạy thành công trong Docker!</h1>
  </React.StrictMode>
)